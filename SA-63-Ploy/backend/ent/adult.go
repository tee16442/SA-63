// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/Sujitnapa21/app/ent/adult"
	"github.com/facebookincubator/ent/dialect/sql"
)

// Adult is the model entity for the Adult schema.
type Adult struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Amount holds the value of the "Amount" field.
	Amount int `json:"Amount,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AdultQuery when eager-loading is set.
	Edges AdultEdges `json:"edges"`
}

// AdultEdges holds the relations/edges for other nodes in the graph.
type AdultEdges struct {
	// Books holds the value of the books edge.
	Books []*Books
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BooksOrErr returns the Books value or an error if the edge
// was not loaded in eager-loading.
func (e AdultEdges) BooksOrErr() ([]*Books, error) {
	if e.loadedTypes[0] {
		return e.Books, nil
	}
	return nil, &NotLoadedError{edge: "books"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Adult) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{}, // id
		&sql.NullInt64{}, // Amount
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Adult fields.
func (a *Adult) assignValues(values ...interface{}) error {
	if m, n := len(values), len(adult.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	a.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullInt64); !ok {
		return fmt.Errorf("unexpected type %T for field Amount", values[0])
	} else if value.Valid {
		a.Amount = int(value.Int64)
	}
	return nil
}

// QueryBooks queries the books edge of the Adult.
func (a *Adult) QueryBooks() *BooksQuery {
	return (&AdultClient{config: a.config}).QueryBooks(a)
}

// Update returns a builder for updating this Adult.
// Note that, you need to call Adult.Unwrap() before calling this method, if this Adult
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Adult) Update() *AdultUpdateOne {
	return (&AdultClient{config: a.config}).UpdateOne(a)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (a *Adult) Unwrap() *Adult {
	tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Adult is not a transactional entity")
	}
	a.config.driver = tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Adult) String() string {
	var builder strings.Builder
	builder.WriteString("Adult(")
	builder.WriteString(fmt.Sprintf("id=%v", a.ID))
	builder.WriteString(", Amount=")
	builder.WriteString(fmt.Sprintf("%v", a.Amount))
	builder.WriteByte(')')
	return builder.String()
}

// Adults is a parsable slice of Adult.
type Adults []*Adult

func (a Adults) config(cfg config) {
	for _i := range a {
		a[_i].config = cfg
	}
}
